import * as decorator from "./lib/api_configuration/types/interface_request/decorator";
import {consumeInterface} from "./consumeInterface";

export const api_configuration = {
	"types": {
		"interface request": {
			"decorator": decorator
		}
	}
};
export {consumeInterface};
